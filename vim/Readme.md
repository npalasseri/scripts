### How to setup the Vim for a new machine
- Install fzf plugin `brew install fzf`
- Download and execute `setup-pathogen.sh` from this directory
- move the `.vimrc` to the $HOME directory of the machine.
